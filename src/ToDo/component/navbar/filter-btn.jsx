function FilterBtn({isComplete}){

    const filterValue = ({ todos, setTodos }) => {
        
    }

    return(
        <div>
          <button className="btn btn-info" onClick={filterHandler}>All</button>
        </div>
    )
}

export default FilterBtn